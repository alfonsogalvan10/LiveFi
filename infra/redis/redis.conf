# Redis — LiveFi local overrides
bind 0.0.0.0
protected-mode no
port 6379

appendonly yes
appendfsync everysec

maxmemory 512mb
maxmemory-policy allkeys-lru

# Enable keyspace notifications for LightTTL cache invalidation
notify-keyspace-events "Ex"

save 900 1
save 300 10
